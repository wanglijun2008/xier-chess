# 中国象棋盲人听棋对弈软件 — 移动端设计文档

## 1. 项目概述

### 1.1 目标
为视障用户设计一款**手机端**无障碍中国象棋应用，通过**语音播报 + 触摸手势 + 语音指令**完成听棋与对弈。

### 1.2 核心原则
- **听觉优先**：所有信息通过语音合成（TTS）输出，不依赖视觉
- **触摸可达**：全部操作通过手势和触摸完成，无需精确点击
- **语音指令**：支持语音输入走棋指令（如"炮二平五"）
- **容错友好**：支持撤销、确认、重复播报等操作
- **屏幕阅读器兼容**：完整适配 iOS VoiceOver 和 Android TalkBack

---

## 2. 功能模块

### 2.1 听棋模式（观战）
- 接收棋谱（PGN/ICCS格式）或实时对局流
- 每步棋语音播报："红方，炮二平五"
- 可选：播报当前完整局面

### 2.2 对弈模式（人机/人人）
- 用户通过触摸手势或语音指令走棋
- 支持人机对弈（内置AI引擎）
- 支持本地双人对弈（轮流操作）
- 支持在线对弈（后续版本）

### 2.3 局面浏览
- 加载残局/开局库
- 左右滑动逐步前进/后退浏览

---

## 3. 交互设计（移动端）

### 3.1 棋盘坐标系统
采用中文坐标描述，与真实象棋术语一致：

```
  九 八 七 六 五 四 三 二 一    （黑方视角，从上到下：第10~1线）
  一 二 三 四 五 六 七 八 九    （红方视角，从下到上：第1~10线）
```

- 红方用中文数字：一二三四五六七八九
- 黑方用阿拉伯数字：123456789
- 走法描述：进/退/平 + 目标位置

### 3.2 触摸交互方式

#### 方式一：触摸选子 + 手势走棋（主要方式）

| 手势 | 功能 |
|------|------|
| 单击棋盘交叉点 | 选中/落子（选中棋子后语音播报棋子信息） |
| 选中后上滑 | 向前（进）移动 |
| 选中后下滑 | 向后（退）移动 |
| 选中后左滑 | 向左（平）移动 |
| 选中后右滑 | 向右（平）移动 |
| 选中后双击 | 确认走棋 |
| 双指点击棋盘 | 播报当前位置棋子信息 |
| 三指点击 | 播报当前局面 |

**操作流程：**
1. 触摸某个交叉点 → 语音播报："红方，炮，位于二路第三线"
2. 滑动手势指定方向和步数 → 语音播报："炮二平五，确认？"
3. 双击确认 / 三指点击取消

#### 方式二：语音指令走棋（辅助方式）

用户直接说出棋谱指令，如：
- "炮二平五" → 系统解析并执行
- "马八进七" → 系统解析并执行
- "车一进一" → 系统解析并执行

系统执行后语音确认："炮二平五，已走"

#### 方式三：列表选棋（精细操作）

对于不方便在棋盘上精确触摸的用户，提供**棋子列表模式**：
- 语音播报当前方所有棋子及位置
- 左右滑动在棋子列表中切换
- 点击选中后，再滑动选择目标位置

### 3.3 功能按钮布局

屏幕底部固定工具栏（大按钮，带语音标签）：

| 按钮 | 功能 | 图标描述 |
|------|------|----------|
| 新局 | 开始新对局 | 棋盘图标 |
| 悔棋 | 撤销上一步 | 后退箭头 |
| 提示 | AI提示最佳走法 | 灯泡图标 |
| 播报局面 | 语音播报完整局面 | 喇叭图标 |
| 重复 | 重复播报上一步 | 循环箭头 |
| 模式 | 切换触摸/语音/列表模式 | 切换图标 |
| 设置 | 语速、音效等设置 | 齿轮图标 |
| 帮助 | 语音播报操作指南 | 问号图标 |

### 3.4 语音播报规范

**触摸选子时：**
> "红方，炮，位于二路第三线"

**手势走棋时：**
> "炮二平五，确认？"

**确认后：**
> "炮二平五，已走"

**吃子时：**
> "炮二平五，吃掉黑方卒"

**将军时：**
> "将军！红方车四进三"

**被将时：**
> "您被将军，请应将"

**绝杀/和棋：**
> "将死！红方获胜" / "和棋"

**播报局面（三指点击或按钮）：**
> "红方：帅在五路底线，双车分别在四路和六路...黑方：将在5路顶线..."

**语音指令模式：**
> "请说出走棋指令" → 用户说"炮二平五" → "炮二平五，确认？"

---

## 4. 技术架构

### 4.1 技术方案：PWA（渐进式Web应用）

选择 PWA 作为首选方案，理由：
- 跨平台（iOS + Android 通用）
- 无需应用商店审核即可分发
- 可安装到手机桌面，体验接近原生
- 支持 Web Speech API 进行语音播报和语音识别

**技术栈：**
- **HTML5 + CSS3 + JavaScript**（单页应用）
- **Web Speech API** — SpeechSynthesis（语音播报）
- **Web Speech API** — SpeechRecognition（语音指令识别）
- **Touch Events API** — 触摸手势处理
- **Service Worker** — 离线缓存，无网络也能用
- **LocalStorage / IndexedDB** — 棋局持久化存储

### 4.2 备选方案：原生 App

如需更深度的系统集成：
- **Android**：Kotlin + Jetpack Compose + Android TTS + 语音识别
- **iOS**：Swift + SwiftUI + AVSpeechSynthesizer + SiriKit
- 优势：更好的屏幕阅读器集成、后台运行、推送通知

### 4.3 核心引擎
- **棋盘状态管理**：10×9 二维数组
- **走法生成器**：按棋子类型生成合法走法
- **走法校验器**：检查蹩马腿、塞象眼、将帅对面等
- **棋谱解析器**：解析中文棋谱指令为坐标移动
- **AI引擎**：Minimax + Alpha-Beta剪枝（可选集成开源引擎如Pikafish）

### 4.4 棋谱格式
- 支持 ICCS 坐标格式（如 H3-E3）
- 支持中文棋谱格式（如 炮二平五）
- 导入/导出标准 PGN

---

## 5. 棋子走法规则

| 棋子 | 走法 | 特殊规则 |
|------|------|----------|
| 将/帅 | 九宫内一步直行 | 将帅不能对面（飞将） |
| 士/仕 | 九宫内一步斜行 | — |
| 象/相 | 走田字 | 不能过河；塞象眼不能走 |
| 马 | 走日字 | 蹩马腿不能走 |
| 车 | 直线任意格 | 不能越子 |
| 炮 | 移动同车 | 吃子必须隔一子（炮架） |
| 兵/卒 | 未过河只能前进 | 过河后可左右移动 |

---

## 6. 无障碍适配（移动端）

### 6.1 屏幕阅读器适配
- **iOS VoiceOver**：每个棋盘交叉点设置 ARIA label，如"二路第三线，红方炮"
- **Android TalkBack**：同上，确保焦点顺序合理
- 支持**转子手势**快速切换棋子列表

### 6.2 触摸区域优化
- 每个交叉点触摸热区扩大到 **44×44pt** 以上（Apple HIG 推荐最小值）
- 手势操作容差宽松，滑动方向识别不要求精确角度

### 6.3 语音设置
- 语速可调（0.5x ~ 2.0x）
- 支持选择系统 TTS 语音（男声/女声）
- 中文语音引擎优先

### 6.4 音效反馈
- 落子声：触摸确认时
- 吃子声：特殊音效区分
- 将军提示音：短促警示音
- 错误提示音：非法走法时

### 6.5 视觉辅助（低视力用户）
- 高对比度棋盘主题（黑底白线/黄线）
- 棋子可选大字体模式
- 当前选中棋子高亮放大
- 可走位置用色块标注

---

## 7. 界面布局（手机竖屏）

```
┌─────────────────────────┐
│  状态栏：当前回合/轮到谁  │
├─────────────────────────┤
│                         │
│                         │
│      象 仕 将 仕 象      │
│    卒                卒  │
│                         │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  │  ← 楚河 汉界
│                         │
│    兵                兵  │
│      相 帅 仕 相         │
│                         │
│                         │
├─────────────────────────┤
│  工具栏（大按钮+语音标签） │
│ [新局][悔棋][提示][播报]  │
│ [重复][模式][设置][帮助]  │
└─────────────────────────┘
```

- 棋盘占屏幕主要区域
- 交叉点用触觉反馈（振动）标记
- 工具栏按钮足够大，间距宽松

---

## 8. 开发路线图

| 阶段 | 内容 |
|------|------|
| v0.1 | 棋盘触摸渲染 + 手势选子走棋 + 基础语音播报 |
| v0.2 | 完整规则引擎 + 合法走法校验 + 屏幕阅读器适配 |
| v0.3 | 语音指令走棋 + 人机对弈（基础AI） |
| v0.4 | 棋谱导入/导出 + 局面浏览 + PWA离线支持 |
| v0.5 | 残局库 + 开局库 + 在线对弈 |
| v1.0 | 完整发布，含音效、盲文显示器支持 |

---

## 9. 测试要点

- [ ] VoiceOver 下所有操作可完成
- [ ] TalkBack 下所有操作可完成
- [ ] 触摸热区足够大，不误触
- [ ] 语音播报清晰，语速可调
- [ ] 语音指令识别准确率 > 90%
- [ ] 离线模式可正常使用
- [ ] 不同屏幕尺寸适配（小屏 iPhone SE ~ 大屏平板）
